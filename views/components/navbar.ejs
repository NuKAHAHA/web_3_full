<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #253e88;"> <!-- Изменение фона на розовый цвет -->
    <div class="container">
      <a class="navbar-brand me-2" href="/">
        <img
          src="/images/msg1097562283-343041.jpg"
          height="48"
          alt="Cloud logo"
          loading="lazy"
          style="margin-top: -1px; border-radius: 20%; "
        />
      </a>
  
      <button
        data-mdb-collapse-init
        class="navbar-toggler"
        type="button"
        data-mdb-target="#navbarButtonsExample"
        aria-controls="navbarButtonsExample"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <i class="fas fa-bars"></i>
      </button>
  
      <div class="collapse navbar-collapse" id="navbarButtonsExample">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link <% if (activePage === 'home') { %>active fw-semibold text-primary<% } %>" href="/">Home</a>
          </li>
          <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link <% if (activePage === 'history') { %>active fw-semibold text-primary<% } %>" href="/history">History</a>
            </li>
            
                <li class="nav-item">
                  <a class="nav-link <% if (activePage === 'admin') { %>active fw-semibold text-primary<% } %>" href="/admin">Admin Panel</a>
                </li>
            <li class="nav-item">
                <a class="nav-link <% if (activePage === 'team-info') { %>active fw-semibold text-primary<% } %>" href="/team-info">Football Team</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <% if (activePage === 'fooballNews') { %>active fw-semibold text-primary<% } %>" href="/football-news">Football News</a>
          </li>
          <% } %>
        </ul>
          <div class="d-flex ms-auto">
            <% if (!user) { %>
              <a href="/login" class="btn btn-primary">Login</a>
              <a href="/signup" class="btn btn-secondary ml-2">Sign up</a>
            <% } else { %>
              <a href="<%= user.is_admin ? '/admin' : '' %>" class="btn btn-primary rounded-3"><%= user.username %></a>&nbsp;&nbsp;
              <a href="/logout" class="btn btn-secondary">Logout</a>
            <% } %>
        </div>
          <a
            class="btn btn-dark px-3"
            href="https://github.com/NuKAHAHA/web_3full"
            role="button"
            target="__blank"
          ><i class="fab fa-github"></i></a>
        </div>
      </div>
      
    </div>
    
</nav>
<hr class="m-0 p-0" style="border-top: 2px solid #253e88;" />
  